# Copie este arquivo para .env e preencha os valores antes de subir o servidor.
# O .env real nunca deve ser commitado no repositório.

DATABASE_URL=sqlite:///./asac.db
SECRET_KEY=troque-por-uma-chave-longa-e-aleatoria
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Chave da API Bluesoft Cosmos (opcional — para consulta de produtos por GTIN)
BLUE_SOFT_COSMOS_KEY=
